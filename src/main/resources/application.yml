spring:
  devtools:
    livereload:
      enabled:true
    restart:
      enabled:false
  freemarker:
    cache: false


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  # show_sql: true

logging.level:

  org.hibernate.SQL: debug
# org.hibernate.type: trace